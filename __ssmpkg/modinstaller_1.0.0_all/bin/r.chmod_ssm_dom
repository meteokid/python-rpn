#!/bin/ksh
. r.misc-fn.dot
if [[ $# != 2 ]] ; then
   echo "USAGE: ${0##*/} PERM DOM"
   exit 1
fi
if [[ ! -d $2 ]] ; then
   echo "USAGE: ${0##*/} PERM DOM"
   echo "       DOM is not a directory: $2"
   exit 1
fi
chmod_ssm_dom $1 $(true_path $2)
